Your previous output was invalid.

Return JSON only.
Do not include markdown.
Do not include explanations.
Do not include comments.

Repair the output so it follows the required minimal ELG JSON schema exactly.
Use only these ELG fields:
- `kind`
- `name`
- `inputs` (only for non-leaf nodes)
